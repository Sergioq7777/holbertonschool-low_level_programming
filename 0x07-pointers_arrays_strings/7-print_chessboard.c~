#include "holberton.h"

/**
 * *_strpbrk - check the code for Holberton School students.
 *@a: char
 * Return: Void.
 */

void print_chessboard(char (*a)[8])
{

	int b, c;
	for(b = 0 ; a[b] != '\0'; a++)
	{
		for(c = 0 ; a[b] != '\0'; c++)
			;
	}
	return (a);
}
