#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int b;

	for (b = '1'; b <= '9'; b++)
		putchar(b);
	putchar('\n');
	return (0);
}
